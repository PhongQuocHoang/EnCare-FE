import React from 'react';
import BookingInformation from '../components/Home/BookingInfor/BookingInformation';

const BookingScreen = ({ navigation }) => {
    return <BookingInformation navigation={navigation} />;
};

export default BookingScreen;
